import about from './About';
import countdown from './SofCountdown';
import history from './History';

export default {
    'sv': {...about.sv, ...countdown.sv},
    'en': {...about.en, ...countdown.en}
};
